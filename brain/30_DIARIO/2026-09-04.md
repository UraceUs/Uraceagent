---
tipo: diario
data: 2026-09-04
---

# 04/09/2026 — o DocuSign entra em produção

[[DocuSign]] · [[VPS e OpenClaw]] · [[P-12 - Integration Key do DocuSign so nasce em demo]]

Go-live aprovado em menos de 48h. A virada teve três surpresas, todas
registradas em [[DocuSign]] e no P-12:

1. **O par RSA não acompanha a promoção.** Produção nasceu com
   `RSA: None`. Upload da chave pública derivada da privada do VPS —
   mesmo par, sem gerar nada.
2. **Redirect URIs também não.** Cadastrada de novo na produção.
3. **Propagação.** O primeiro Allow de produção falhou com "não existem
   URI de redirecionamento" mesmo com a URI salva. Cinco minutos depois,
   passou.

Prova pela própria ferramenta do agente: `"ambiente": "PRODUÇÃO"`,
**50 envelopes** em 120 dias. O primeiro da lista, waiver parental do
Bruce Duthan em maio, assinada.

O que muda para a operação: a varredura diária das 07:30 passa a cruzar
[[Asana]] **e** DocuSign. As 3 waivers paradas desde junho
([[P-07 - Waivers paradas desde junho]]) saem do acompanhamento manual.
Envio segue atrás de `APLICAR=1` — decisão do dono, não do calendário.

## 14:10 — a primeira varredura completa, conferida

Com as duas fontes ligadas, o `urace-waivers` cruzou [[Asana]] e
[[DocuSign]] pela primeira vez. Cada achado foi conferido por leitura
independente antes de entrar aqui:

| Achado do agente | Conferido |
|---|---|
| 🔴 **Renato Frota Pionti**, serviço **05/09**, menor (14), waiver **nunca enviada** — responsável Rafael Pionti | ✅ nenhum envelope para ele ou o responsável no último ano; subtarefa aberta |
| ⏳ Matthew Hubbard expira 29/09 | ✅ |
| ⚠️ Envelope anterior do Hubbard foi para `misterhub**bb**ard@gmail.com` e **devolveu** (`550 no such user`, 27/05); o reenvio de 01/06 está certo e segue `sent` | ✅ `autoresponded`, com o SMTP na resposta |
| Nya Amankwa: waiver enviada hoje pelo Italo, para o serviço de 27/09 | ✅ |
| William Fatutta (19/09), Rosalvo (26/09, também `autoresponded`) perto de expirar | não conferido — fora da janela que eu li; o agente leu 120 dias |

O agente **não enviou nada**: `APLICAR=0`, e a trava manda confirmar.
Escalou. Relatório em `relatorios/waivers-2026-09-04.md`, no workspace.

**O que a operação aprende hoje:** `autoresponded` é um estado que a
tela do DocuSign não grita, e é um envelope que nunca vai ser assinado.
A varredura passa a olhar esse status como falha de e-mail, não como
"aguardando".

**Decisão pendente do dono:** a waiver do Renato precisa sair **hoje** —
o serviço é amanhã. Ou o Italo manda pela tela, como fez com a Nya, ou
libera `APLICAR=1` para o agente mandar. Ver [[Escalonamento]].

E a tarefa do Renato veio do **chat de teste de 02/09**: 12 subtarefas,
`Practice OKC`, responsável certo, comentário `[IA ADM]`. O prompt de
`adminai/deploy/prompts/chat-agendar-sessao.txt` produz tarefa que a
automação lê.

## Duas lacunas fechadas pelo dono

- **Renato é VIP**, e **VIP dispensa waiver**. Virou regra em
  [[PARAMETROS]] com lista única; [[Rafael Pionti]] entra como cliente.
- **RACES não entra na varredura de waiver.** Só as colunas dos dias.

## Tarde — o Google entra (as duas caixas)

Cliente OAuth **interno** ao Workspace, criado pela extensão — sem
revisão do Google. Consentimento uma vez por caixa, pelo
`google_auth.py`: `urace@` (41.876 threads) e `support@` (4.460).
Servidor MCP próprio, 9 ferramentas, **sem envio**. Prova pela
ferramenta: as duas contas `ok: true`.

Três tropeços do instalador no mesmo dia, todos corrigidos: verificação
do cérebro abortava a instalação; `brain_health` procurava `brain/` no
diretório de quem chamou; e os timers eram ligados **antes** dos MCP —
timer atrasado dispara na hora, e a triagem das 11:34 rodou sem Gmail.
O agente reportou isso em vez de inventar caixa vazia.

Do arquivo do Chase saiu um fato vivo: o **horário de funcionamento**
(quarta a domingo, 8h–13h), que o cérebro não tinha — e um conflito
com a coluna TUESDAY (C-02).

## 11:53 — a primeira triagem de e-mail real

Duas caixas lidas. 8 threads novas, todas propaganda/notificação, cada
uma com marcador da taxonomia, nenhum rascunho, nada gravado
(`APLICAR=0`). O agente apontou sozinho que **`support@` não tem
`wNews`** — e que a regra em código o impede de arquivar lá. Clique do
dono, não da IA. Ver [[Gmail]].

**As três conexões estão de pé: [[Asana]], [[DocuSign]] em produção,
[[Gmail]] nas duas caixas.** Só [[QuickBooks]] espera — a fila da Intuit.

## O painel passa a existir de verdade

A seção **Em progresso** que o dono pediu está no ar, lendo os
relatórios das rotinas. Mas o achado do dia foi outro: **o gerador do
painel nunca tinha rodado**. Quebrava na primeira linha de CSS, porque
o template inteiro passava por `.format()` e `{--paper:…}` era lido como
campo. A página aprovada em 01/09 era desenho feito à mão.

Segundo achado, meu: o renderizador de markdown que escrevi funcionava
com os relatórios que **eu** inventei para testar, e falhava com os que
o agente escreve — ele quebra frases no meio, e negrito atravessando a
quebra nunca fechava. Só apareceu porque pedi a página real do VPS em
vez de confiar no meu próprio teste.

Virou suíte: `adminai/painel/tests/test_md.py`, um caso por padrão que
quebrou. Ver [[VPS e OpenClaw]].

## Command Center: frontend, deploy e dois bugs pegos antes de subir

O frontend inteiro do Command Center existe e roda: login, shell,
Dashboard, Precisa de atenção, Clientes, Cliente 360, Serviços, Waivers,
E-mails, AI Command, Aprovações, Atividade, Integrações, Políticas,
Usuários, Auditoria, Minha conta, ⌘K. Identidade do Pit Wall.

Teste em navegador de verdade (Chromium + backend + banco de teste)
pegou o que o pytest não pegava: o SQLite recusando a conexão em outra
thread (500 em toda rota; `check_same_thread=False`) e o "Sair" que
devolvia o próximo login à última página. Nenhum dos dois apareceria em
teste de unidade. Lição repetida: prova é o sistema inteiro rodando.

Deploy escrito em `adminai/deploy/command_center/`: unit, script com
venv, build, testes, primeiro ADMIN, Caddy `/ops*`, prova real. Falta o
dono rodar no VPS. Ver [[VPS e OpenClaw]].

## Command Center no ar
Deploy no VPS por Eduardo Resende (primeiro ADMIN). Três paradas antes
de subir, todas na etapa de testes ou cadastro: teste "sem credencial"
falando com o Gmail real (conftest isola HOME), senha mínima 10 →
decisão do dono: 5 ([[D-2026-09-04 - Senha do Command Center com minimo de 5]]),
e a sessão `cc` do tmux sobrando. Prova real passou; o ❌ do script era
o `/painel/` em 404, que nunca esteve no ar. Próximo: "Sincronizar
agora" para encher os espelhos.

## Command Center no ar e segunda rodada de pedidos do dono

- **Deploy real**: `https://urace-bridge.duckdns.org/ops/` responde 200 com login; API sem sessão 401; primeiro ADMIN é **Eduardo Resende** (`eduardoffresende@gmail.com`). Três paradas no caminho: terminal do Lightsail caindo no silêncio do `pip` (script passou a anunciar cada passo), teste "sem credencial" falando com o Gmail real do VPS (`conftest.py` isola HOME), senha mínima 10 → **5** por decisão do dono.
- **Pedidos atendidos na sequência**: olho para ver a senha no login; abas por sistema (**Asana** com calendário, quadro e lista; **DocuSign** com envelopes e modelos; **Gmail** por caixa com "marcar tratado"; **QuickBooks** honesto em stand-by); **ocultar** aviso de atenção (não apaga a origem; registra quem/por quê; restaurável); **Clientes pelo nome do piloto**, com card completo em janela; sincronia passou a ler o **quadro inteiro** (menos "Matt tasks"), inclusive Finished Services concluídas — histórico desde a criação do projeto — e roda em segundo plano.
- Bug pego pelo teste de navegador: efeito de busca com `sp` velho apagava `?open=` do card. Corrigido lendo `window.location.search`.
