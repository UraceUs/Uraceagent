---
tipo: diario
data: 2026-09-18
---

# 18/09/2026

## O menu lateral agora fecha, e o layout passa a respeitar a tela

Pedido dele: *"esse menu lateral ele seja possível de abrir e fechar. E quando ele estiver
fechado, todo o restante do layout acompanhe o tamanho da tela… tanto no computador quanto no
celular. Eu percebi que no celular tem alguns layouts que não estão bem otimizados"* — e,
antes de mexer em código, **opções para ele aprovar**. Mesmo método do login: canvas com as
alternativas, ele escolhe.

Foram três opções para o estado fechado, com o aberto igual ao de hoje: **A** trilho de
ícones de 68px; **B** o menu somindo por completo; **C** trilho que, ao encostar o mouse,
abre o menu inteiro **por cima** do conteúdo. Ele escolheu a **C** —
[[D-2026-09-18 - Menu em trilho que revela, e a tela manda no layout]].

Três decisões saíram junto: o estado fica **guardado por navegador** (quem fecha encontra
fechado), a largura em monitor grande é **limitada só nas páginas de leitura**, e o resto
(quadro, tabela larga, calendário, chat) usa a tela inteira.

## O que o Chromium de 390px mostrou

A varredura de rolagem lateral já existia (`command_center/tests/e2e/celular.mjs`) e cobria
12 rotas; passou a cobrir **25** e a acusar também alvo de toque menor que 34px. Nenhuma tela
estourava a largura — o problema era outro, e só apareceu olhando as capturas:

- **O título de toda página ia para a direita no celular.** O layout já virava coluna em
  ≤700px, mas o `align-items:flex-end` da camada de vidro (que no computador alinha o título
  com os botões pela base) continuava valendo — e, em coluna, isso empurra tudo para a
  direita. Passou a valer só em ≥701px. Uma linha de CSS que consertou "Asana", "Usuários",
  "Gmail", "Oportunidades" e todas as outras.
- **Calendário do Asana ilegível**: 7 colunas de 50px, com as tarefas cortadas em "T…". No
  celular a aba **Lista** passou a ser a padrão (o calendário fica a um toque, e a escolha vai
  para a URL), e o mês, quando aberto, rola dentro do quadro com coluna mínima de 60px.
- **Lista de tarefas do Asana** virou cartão com rótulo por campo (`tbl rsp`), em vez de
  tabela cortada na direita.
- Rede de segurança: `#root{overflow-x:clip}` (não quebra o topo grudado, diferente de
  `hidden`), imagem e `pre` limitados à largura, e tabela larga que não virou cartão rolando
  dentro do próprio quadro em vez de empurrar a página.
- Alvo de toque: botão com 40px de altura mínima e campo com 44px e fonte 16px no celular
  (o iOS não dá zoom com 16px).

## Um teste que quebrou sozinho

`test_dashboard_e_atencao_contextual` falhou sem ninguém ter mexido nele: a atenção "cliente
escreveu" só olha os **últimos 14 dias**, e o e-mail do teste tinha data fixa em 04/09 — passou
até 17/09 e quebrou no dia 18. A regra está certa; a data do cenário virou relativa
(`HOJE - 3 dias`). 207 testes passando.

## As travas, respondidas em uma tarde

Ele pediu a lista do que está travado e precisando de ação nossa, e depois pediu para
**responder dentro de cada tema**. Virou uma página com as 19 travas — o que é, por que
trava, botões de resposta rápida e campo livre — e ele respondeu **todas**:
[[D-2026-09-18 - As travas respondidas, uma a uma]].

Antes de listar, conferi cada registro na fonte — e achei um que estava aberto sem
precisar: **P-09** (o conector do Asana não sobe anexo) foi resolvido em **02/09**, quando
o MCP próprio ganhou `asana_anexar_arquivo`. Ninguém tinha movido a nota. É a segunda vez
na semana que um problema resolvido continua figurando como travamento; o hábito agora é
conferir no código antes de pôr na lista.

**O que entrou no mesmo dia:**

- **O azul oficial: `#0057B4`.** O painel usava `#1E5BC6`, que eu tinha tirado da foto do
  kart por falta do hex oficial. Uma pegadinha no caminho: o azul oficial é escuro, e
  `a{color:var(--accent)}` deixava link ilegível no tema preto (contraste 2,7:1). Então
  `--accent` ficou para **preenchimento** (botão com texto branco: 6,9:1) e o texto passou
  a usar `--accent-ink` — `#7FB3F5` no escuro, `#00417F` no claro. Cinco lugares trocados:
  link, botão de ajuda, sugestão, olho da senha e chip de destaque.
- **`aposentar_painel.sh`**: tira o bloco `/painel` do Caddy com backup, valida, restaura se
  reprovar, desliga as unidades do Pit Wall e prova no fim que `/ops/` continua 200. Não
  apaga nada — `servir_painel.sh` republica se um dia quiser de volta.

**O que ficou com plano antes de fazer:** o catálogo do QuickBooks (896 itens — lista
revisada antes de mexer em preço), o `APLICAR=1` (a lista do que o agente passa a escrever
sozinho, antes de ligar), a higiene do Asana e o Order Number ("o que for seguro" precisa
virar definição em código), e a Rate Card do Drive como fonte lida, não copiada.

**Duas regras novas entraram em [[PARAMETROS]]:** quando cobrar o security deposit (primeiro
serviço, ou depois que o anterior voltou) e que a Rate Card do Drive é a fonte de preço.
[[Conflitos e lacunas]] ficou com **uma** lacuna aberta — U-02, o fornecedor do
`whitesoldier205@gmail.com`, que nem estava na lista.

## A Rate Card passa a mandar no preço, sozinha

Ele respondeu a página de preços inteira e aprovou o caminho: lista agora, ação no painel e
**leitura semanal**. Três coisas saíram disso.

**O cruzamento achou dano, não só diferença.** Quatro itens do catálogo estão com o preço
mil vezes menor — `State Events` cadastrado como **$2,40** no lugar de $2.400, `FLKC Arrive
and drive` como $4,10, `Summer Camp Jr/Sr 2t` como $3,63, `Travel Fee` como $0,85. Parece
separador de milhar comido numa importação. Invoice feita por esses itens cobra centavos.
Os valores certos vieram dele, item a item.

**Duas armadilhas de parser, ambas reais.** A planilha mistura `$1,250` e `$2.756,90` na
mesma coluna — o primeiro parser leu 2.756,90 como *dois e setenta e seis*, e só apareceu
porque o resultado ficou absurdo na tela. E um item do catálogo está escrito **"Pratice"**,
o que fura qualquer casamento por palavra. As duas viraram teste.

**O casamento por semelhança não entra em produção.** Ele chegou a propor o preço de 3 dias
para o item de 5 dias, e o de dia avulso para o pacote de 4. Então o que vale é um **mapa
revisado** (`db/ratecard_mapa.json`), com as respostas dele: 10 pares, 4 correções e 38
famílias marcadas como criar ou ignorar. O que não está no mapa **não é escrito** — vira
pergunta em "Precisa de atenção".

No ar depois do deploy: `GET /ops/api/precos/plano` (o que mudaria, sem tocar),
`POST /ops/api/precos/aplicar` (gerente) e a rotina **`ratecard_semanal`**, que roda todo dia
às 07:30 e só age na segunda. Escrita de preço com três recusas: peça, faixa e item inativo.
236 testes.
