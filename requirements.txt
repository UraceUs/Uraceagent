google-cloud-firestore>=2.16
python-dotenv>=1.0
openpyxl>=3.1
PyYAML>=6.0
anthropic>=0.40
# API (opcional — só se rodar o orchestrator como serviço)
fastapi>=0.110
uvicorn>=0.29
# Catálogo via Google Sheets (opcional — o fixture dispensa)
google-api-python-client>=2.120
google-auth>=2.28
# Embeddings reais (opcional — o provider fake dispensa)
voyageai>=0.3
